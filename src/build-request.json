{
  "kind": "build_request",
  "title": "Add configurable owner contact email and WhatsApp number across the site",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-19",
      "text": "Create a single source of truth on the frontend for the site owner’s contact details (at minimum: contact email and WhatsApp number) that persists in localStorage and provides safe defaults when not set.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "can i add mine mail id and whats app number"
        ]
      },
      "acceptanceCriteria": [
        "A new frontend utility (e.g., in frontend/src/lib) exists to get/set contact email and WhatsApp number using localStorage.",
        "If localStorage values are missing or invalid, the app falls back to the current placeholder WhatsApp number (+919876543210) and an empty/missing email without crashing.",
        "WhatsApp number handling supports common input formats (with spaces, +, dashes) and derives a wa.me-safe value (digits only) for link generation."
      ]
    },
    {
      "id": "REQ-20",
      "text": "Add an Owner Dashboard section that allows the owner to view and update the contact email and WhatsApp number, gated by the existing Owner Mode flow.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "can i add mine mail id and whats app number"
        ]
      },
      "acceptanceCriteria": [
        "OwnerPage includes an additional tab or section (in English) for editing contact details, visible only when Owner Mode is enabled via the existing OwnerGate.",
        "The form includes fields for Email and WhatsApp Number, with basic validation (email format; WhatsApp must contain at least one digit).",
        "Saving updates persists the values in localStorage and shows a success confirmation using existing UI feedback patterns (e.g., toast and/or inline message).",
        "Refreshing the page retains the saved contact details and re-populates the Owner Dashboard fields."
      ]
    },
    {
      "id": "REQ-21",
      "text": "Replace hardcoded WhatsApp placeholders in the header, footer, and Home page Contact section so they render and link using the saved owner contact details (email + WhatsApp).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "can i add mine mail id and whats app number"
        ]
      },
      "acceptanceCriteria": [
        "frontend/src/components/SiteHeader.tsx no longer uses a hardcoded whatsappNumber constant; it uses the stored WhatsApp value and generates a valid https://wa.me/<digits> link.",
        "frontend/src/components/SiteFooter.tsx no longer uses a hardcoded whatsappNumber constant; it uses the stored WhatsApp value and generates a valid https://wa.me/<digits> link.",
        "frontend/src/pages/HomePage.tsx no longer uses a hardcoded whatsappNumber constant; it uses the stored WhatsApp value for the button link and the displayed number.",
        "Home page Contact section displays the owner email (as a clickable mailto: link) when an email is set; if not set, it omits the email line (no placeholder text).",
        "All user-facing labels added/changed are in English."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui (compose existing UI components only).",
    "Do not modify any frontend immutable paths listed in SYSTEM_CONTEXT.",
    "Do not introduce external authentication or third-party integrations beyond the existing Owner Mode local toggle and current UI patterns.",
    "Do not route images through the backend; no image changes are required for this request."
  ],
  "nonGoals": [
    "Persisting contact details in the Motoko backend canister state (this change should be frontend/localStorage-based).",
    "Adding or changing social media links (Facebook/Instagram/LinkedIn) beyond the requested email/WhatsApp.",
    "Changing inquiry submission fields or backend inquiry storage behavior."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}